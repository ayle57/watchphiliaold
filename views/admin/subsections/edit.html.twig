
{% extends 'templates/admins/nav/default.html.twig' %}

{% block title %}Editer Sous Sections{% endblock %}

{% block section_title %}Editer une Sous Section{% endblock %}

{% block content %}
    <h2><a href="/admin/subsections/"><button class="btn btn-secondary">Retourner aux Sous Sections</button></a></h2>
    <div class="container">
        <form action="" method="POST">
            {% include 'templates/input.twig' with {
                input_name: 'name',
                input_placeholder: 'Nouveau nom',
                input_id: 'name',
                input_value: subsection.name
            } %}
            <div class="d-flex justify-content-center">
                <button class="btn btn-primary mt-4" name="submit">Mettre à jour</button>
            </div>

        </form>

        {% if errors %}
            <ul class="alert alert-danger mt-4 px-5">
                {% for error in errors %}
                    <li>
                        <span class="text-danger">
                            {{ error }}
                        </span>
                    </li>
                {% endfor %}
            </ul>
        {% endif %}
    </div>
{% endblock %}

